# Real CGM traces

Place the provider-authorized T1DiabetesGranada participant CSV files here.
Each file must be named `<subject_id>.csv` (or
`<subject_id>_processed.csv`) and contain:

- `Time` or `timestamp`
- `CGM` or `glucose_mgdl`

Raw participant-level data are excluded from Git.

